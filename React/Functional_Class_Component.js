// Components let you split the UI into independent, reusable pieces, and think about each piece in isolation.

// A React component takes in PROPS AS INPUT and returns REACT ELEMENT

// Functional Component - Must return a element, prefrably JSX element
// Class Component - 
            // 1. Should extend React.component
            // 2. Should return React element from render function

// Props are passed to above constructor
// A component must not change its props

